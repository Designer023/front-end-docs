// Import React so we can use it's Component class
import React from 'react';

// Create a component that can be displayed in the DOM
class App extends React.Component {
  render() {
    return (
      <div>I am a basic component - App</div>
    )
  }
}

// Export the App component for use in other files that import it
export default App;
